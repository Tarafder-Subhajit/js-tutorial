"use strict"; //treat all code as newer JS version

/* alert() function is used for pop-up print on browser.
But we cannot use this since we are using node engine to run js files.
Web browser has its own js engine where we can use alert command in the console. */

// Data Types: int, 
// bigint, 
// string, 
// boolean, 
// null(in JS, it is a data type as well as stand-alone value. It represents empty value), 
// undefined (variable declared but not its value),
// symbol (used for unique representation)
// object



// typeof() function is used to know the datatype of the variable
console.log(typeof "subha");

console.log(typeof null); // o/p: object
console.log(typeof undefined); o/p; undefined

